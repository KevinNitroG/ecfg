---
phase: 04-file-system-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [FS-01, FS-02, FS-03, FS-04, FS-05]
---

<objective>
Add editorconfig-core-go dependency and implement file system resolution for EditorConfig inheritance detection.

Purpose: Enable the LSP server to traverse parent directories, resolve parent .editorconfig files, and detect property inheritance for diagnostics.

Output: internal/resolver/ package wrapping editorconfig-core-go with inheritance detection.
</objective>

<context>
@go.mod
@internal/parser/ast.go
@internal/validator/schema.go

# From Phase 3 SUMMARY (for context):
# - validator.Schema maps property names to PropertySchema (line 66)
# - AST types defined in internal/parser/ast.go
# - All Phase 1-3 requirements completed

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editorconfig-core-go dependency and create resolver package</name>
  <files>go.mod, internal/resolver/resolver.go</files>
  <action>
1. Run: go get github.com/editorconfig/editorconfig-core-go/v2
2. Create internal/resolver/resolver.go with:
   - NewResolver(rootDir string) *Resolver - takes workspace root
   - Resolve(path string) (*Definition, error) - resolves EditorConfig for a file path
   - Definition struct contains: Raw (map of properties), Files (list of config files in hierarchy)
3. Wrap editorconfig-core-go's GetDefinitionForFilename to traverse parent dirs
4. Ensure resolver stops at root=true and system root (library handles this)
  </action>
  <verify>
go build ./... succeeds
go vet ./internal/resolver/... passes
  </verify>
  <done>
Resolver type can resolve EditorConfig for any file path within workspace, returning merged properties and file list
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement inheritance detection for redundant property diagnostics</name>
  <files>internal/resolver/resolver.go, internal/diagnostic/diagnostic.go</files>
  <action>
1. In resolver.go, add InheritedProperties(path string) (inherited map[string]string, inheritedFrom map[string]string, err)
   - Uses library to get definition for path
   - Walks parent directories to find all .editorconfig files
   - For each property in current file, check if parent also defines it
   - Returns: inherited (props that exist in parent), inheritedFrom (map to source file)
2. Update diagnostic package to emit info diagnostic for redundant properties:
   - In DiagnosticsForFile, after validation, call resolver.InheritedProperties
   - For each property that exists in both current and parent, emit info diagnostic
   - Diagnostic message: "Property '{key}' is redundant; inherits value from {parent_file}"
   - Use SeverityInformation (not Warning - inherited props are valid, just informational)
3. Ensure diagnostic includes file path reference in Message
  </action>
  <verify>
go test ./internal/resolver/... -v
go test ./internal/diagnostic/... -v
  </verify>
  <done>
Server emits info diagnostic when property is redundant (inherited from parent)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test suite with multi-level .editorconfig hierarchies</name>
  <files>internal/resolver/resolver_test.go</files>
  <action>
1. Create internal/resolver/resolver_test.go with test fixtures:
   - Create temp directories with 3-level hierarchy:
     - /root/.editorconfig (root = true, indent_style = space)
     - /root/project/.editorconfig (indent_size = 2)
     - /root/project/src/.editorconfig (end_of_line = lf)
   - Test cases:
     a) Resolve from /root/project/src/file.go - should include all 3 files
     b) Resolve from /root/project/file.go - should include root + project
     c) Stops at root = true (does not go above /root/)
     d) No root = true - should traverse to system root
2. Test inheritance detection:
   - /root/project/src/.editorconfig defines indent_style = space (redundant, from parent)
   - /root/project/src/.editorconfig defines charset = utf-8 (not redundant)
   - Verify InheritedProperties returns correct inherited map
3. Clean up temp directories in test teardown
  </action>
  <verify>
go test ./internal/resolver/... -v -run TestResolver
go test ./internal/resolver/... -v -run TestInheritance
  </verify>
  <done>
Test suite covers: traversal to parent files, stopping at root=true, stopping at system root, inheritance detection
  </done>
</task>

</tasks>

<verification>
- go vet passes for all packages
- golangci-lint reports 0 issues
- All resolver tests pass
- Integration test: manually verify resolution works for a nested file path
</verification>

<success_criteria>
1. Server correctly finds parent .editorconfig files up directory tree (FS-01)
2. Server stops at root = true in parent file (FS-02)
3. Server stops at filesystem root if no root = true found (FS-03)
4. Server emits info diagnostic when property is redundant (inherited from parent) (FS-05)
5. Integration with editorconfig-core-go library works correctly (FS-04)
</success_criteria>

<output>
After completion, create .planning/phases/04-file-system-resolution/04-01-SUMMARY.md
</output>